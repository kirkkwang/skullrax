<div class="card h-100">
  <div class="card-body">
    <h3><%= t('skullrax.dashboard.export.title') %></h3>
    <p class="text-muted"><%= t('skullrax.dashboard.export.instruction') %></p>
    <div class="form-group">
      <textarea class="form-control" rows="4" placeholder="<%= t('skullrax.dashboard.export.placeholder') %>"></textarea>
    </div>
    <button class="btn btn-outline-secondary disabled" disabled>
      <%= t('skullrax.dashboard.export.submit') %> (Coming Soon)
    </button>
  </div>
</div>
